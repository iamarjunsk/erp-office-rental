## 2024-05-24 - Replace Native Alerts & Missing Loading States | Learning: Users experienced layout shifts and missing context due to missing loading spinners on submit buttons, and jarring UX from native `alert()` for form errors. | Action: Added loading spinners with flex layout spacing on submit buttons, disabled state while submitting, and migrated away from native `alert()` calls towards `useToast()` based notifications in shadcn-vue.
