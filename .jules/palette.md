## 2026-04-15 - Authentication Form Accessibility | Learning: Inputs lacked matching 'id'/'for' attributes, password toggles lacked dynamic ARIA labels, and error messages lacked the 'alert' role. | Action: Ensure all form inputs have matching 'id' and 'for' labels, interactive icons use descriptive dynamic 'aria-label' attributes, and error messages utilize 'role="alert"' for screen readers.
