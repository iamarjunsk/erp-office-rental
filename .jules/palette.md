## 2024-05-01 - Add IDs and labels to form inputs in login page | Learning: Form inputs without explicit ID linkage to labels using `for` attributes reduce accessibility and screen reader usability. Icon-only buttons need an `aria-label` for screen reader access. | Action: Added `id` and `for` attributes to explicitly link labels and inputs in login.vue. Added `aria-label` to the password visibility toggle button.
