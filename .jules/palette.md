## 2024-05-24 - Replace Native Alerts with Toast Notifications | Learning: Native `alert()` calls disrupt user flow and feel unpolished in modern web apps, particularly when performing basic CRUD operations like booking creation or deletion. | Action: Replaced native `alert()` calls with shadcn-vue's `useToast` composable in the Bookings page (`app/pages/admin/bookings/index.vue`) to provide non-blocking, integrated feedback for both success and error states, ensuring a smoother, more professional UX.
