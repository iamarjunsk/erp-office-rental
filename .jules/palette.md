## 2024-04-03 - Login Form Accessibility Fixes | Learning: Many login forms miss essential accessibility features like input-label mapping and alert roles for errors, which are crucial for screen reader users. | Action: Ensure all form labels have a `for` attribute connected to the input's `id`, add dynamic `aria-label`s to icon-only buttons like password toggles, and use `role="alert"` for error messages.
