## 2024-05-20 - Replace Native Alerts with Toasts | Learning: Native `alert()` calls disrupt the user flow and styling consistency, especially when displaying multiline backend validation errors. Furthermore, `\n` characters from backend errors do not render properly in shadcn-vue `ToastDescription`. | Action: Replaced native alerts with the `useToast` composable and formatted backend array errors to join with commas (`, `) instead of newlines (`\n`) for clean rendering in toast notifications.
