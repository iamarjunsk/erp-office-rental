## 2024-03-09 - Add aria-label and tooltip to password toggle button | Learning: When using shadcn-vue in a Nuxt project with radix-vue, verify that generated components import from radix-vue rather than reka-ui, and delete `index.ts` files inside UI component folders to prevent Nuxt auto-import warnings ("Two component files resolving to the same name"). | Action: Refactored tooltip components to use `radix-vue`, removed the `index.ts` file, and added `app/lib/utils.ts` for cn() to correctly render the `<UiTooltip>` wrapped password toggle button.
