## 2026-04-13 - Replace Blocking Alerts with Toast Notifications | Learning: Native alert() calls block user interaction and disrupt form submission flows, particularly when multiple validation errors occur. | Action: Use non-intrusive toast notifications (e.g., shadcn's useToast) for error handling and join multiple validation errors with commas instead of newlines for cleaner UI presentation.
