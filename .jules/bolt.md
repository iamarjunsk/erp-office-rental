## 2025-03-01 - Combined Aggregations in Maintenance Stats **Learning:** Django `count()` queries in loops or separate statements can be extremely inefficient compared to a single `aggregate()` with multiple conditional `Count()` objects. **Action:** Always use a single `.aggregate()` call with multiple conditional `Count()` objects for related statistics on the same model to avoid multiple DB roundtrips.
