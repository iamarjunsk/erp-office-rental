## 2025-02-14 - Optimized `MaintenanceRequestViewSet.stats` **Learning:** Optimized 7 individual `.count()` Django database queries into a single `.aggregate()` call using multiple `Count` and `Q` objects. **Action:** Next time when encountering multiple individual aggregates or `.count()` queries on the same model, use a single `aggregate()` with multiple fields to drastically reduce database roundtrips.
